Replace this with the changelog for the release you're about to tag.

A changelog is **required** for every release — the workflow fails if this file
is empty. It's used for the GitHub release body, the auto-update manifest, the
Modrinth version changelog, and the Discord changelog message.
