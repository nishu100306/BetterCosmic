The full mod description (Markdown). Optional — updates only when this file's
content changes.

On change it updates the Modrinth project **body** and the Discord full-description
embed. Leave as-is (or empty) to skip.
