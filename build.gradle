// Root project is an aggregator only — it contains no source or mod of its own.
// The real modules are:
//   :shared        — the shared client library (library mod "bettercosmicshared")
//   :bettercosmic  — the unified mod (BetterPrisons + BetterSky features, each gated to its own
//                    Cosmic network at runtime), which depends on and bundles :shared
//
// Common versions live in gradle.properties. Each module applies fabric-loom itself.
